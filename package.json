{
  "name": "interviewos-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "vercel-build": "NEXT_DIST_ROOT=1 npm run build -w frontend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "cd backend && uv run uvicorn app.main:app --reload --port 8000",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "lint": "npm run lint -w frontend && cd backend && uv run ruff check .",
    "test": "npm run test -w frontend && cd backend && uv run pytest"
  }
}
